// Plugins racine
plugins {
    id 'com.android.application' version '8.5.0' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.0' apply false
}

// Tâche clean personnalisée (facultatif)
task clean(type: Delete) {
    delete rootProject.buildDir
}
